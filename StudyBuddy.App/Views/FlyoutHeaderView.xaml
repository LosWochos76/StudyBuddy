<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    xmlns:viewmodels="clr-namespace:StudyBuddy.App.ViewModels"
    x:Class="StudyBuddy.App.Views.FlyoutHeaderView"
    x:DataType="viewmodels:FlyoutHeaderViewModel">
    <ContentView.Content>
        <StackLayout Margin="10">
            <xct:AvatarView
                Text="AS"
                Source="{Binding User.ProfileImage}"
                Size="50"
                HorizontalOptions="Start"
                TextColor="{AppThemeBinding Dark={StaticResource TextColorDark}, Light={StaticResource TextColor}}"/>
            <Label Text="{Binding User.Name}" FontSize="Large" FontAttributes="Bold"/>
            <Label Text="{Binding User.Nickname}" TextColor="{StaticResource SecondaryFont}" FontSize="Medium"/>
            <Label Text="{Binding FriendsCount, StringFormat='{0} Freunde'}" Margin="0,5,0,5"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>
