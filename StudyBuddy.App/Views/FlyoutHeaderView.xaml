<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    x:Class="StudyBuddy.App.Views.FlyoutHeaderView">
    <ContentView.Content>
        <StackLayout Margin="10">
            <xct:AvatarView
                Text="AS"
                Source="{Binding User.ProfileImage}"
                Size="50"
                HorizontalOptions="Start"/>
            <Label Text="{Binding User.Name}" FontSize="Large"/>
            <Label Text="{Binding User.Nickname}" TextColor="Gray"/>
            <Label Text="20 Freunde" Margin="0,5,0,5"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>
