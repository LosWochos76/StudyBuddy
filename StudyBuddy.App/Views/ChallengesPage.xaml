<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="StudyBuddy.App.Views.ChallengesPage"
    xmlns:viewmodels="clr-namespace:StudyBuddy.App.ViewModels"
    xmlns:fontAwesome="clr-namespace:StudyBuddy.App"
    x:DataType="viewmodels:ChallengesViewModel"
             Style="{StaticResource BaseContentPage}"
             x:Name="page" Title="{Binding Header}">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="QR-Code scannen" Command="{Binding ScanQrCodeCommand}">
            <ToolbarItem.IconImageSource>
                <FontImageSource FontFamily="{StaticResource FontAwesomeSolid}"
                    Glyph="{x:Static fontAwesome:FontAwesomeIcons.Camera}" 
                                 Color="{AppThemeBinding Dark={StaticResource TextColorDark}, Light={StaticResource TextColor}}"/>
           </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <StackLayout.GestureRecognizers>
                <SwipeGestureRecognizer Direction="Right" Command="{Binding ScanQrCodeCommand}" />
            </StackLayout.GestureRecognizers>
            <Frame Padding="5"
                   Margin="4"
                   BackgroundColor="{StaticResource WindowBackgroundColor}"
                   HasShadow="False"
                   BorderColor="Black"
                   CornerRadius="8"
                   HeightRequest="45">
                <Frame.IsVisible>
                    <OnPlatform x:TypeArguments="x:Boolean"
                                iOS="False"
                                Android="True" />
                </Frame.IsVisible>
                <SearchBar TextColor="{AppThemeBinding Dark={StaticResource TextColorDark}, Light={StaticResource TextColor}}"
                           x:Name="searchBar"
                           TextChanged="searchBar_TextChanged">
                    <SearchBar.IsVisible>
                        <OnPlatform x:TypeArguments="x:Boolean"
                                    iOS="False"
                                    Android="True" />
                    </SearchBar.IsVisible>
                </SearchBar>

            </Frame>
            <SearchBar TextColor="{AppThemeBinding Dark={StaticResource TextColorDark}, Light={StaticResource TextColor}}"
                       x:Name="searchBarIos"
                       TextChanged="searchBar_TextChanged">
                <SearchBar.IsVisible>
                    <OnPlatform x:TypeArguments="x:Boolean"
                                iOS="True"
                                Android="False" />
                </SearchBar.IsVisible>
            </SearchBar>
            <RefreshView 
                IsRefreshing="{Binding IsRefreshing}"
                Command="{Binding RefreshCommand}"
                Padding="8">
                <CollectionView ItemsSource="{Binding Challenges}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:ChallengeViewModel">
                            <ContentView>
                                <StackLayout>
                                    <Grid Padding="10" x:Name="Item">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Label Style="{StaticResource IconLabel}" Grid.Column="0" Grid.Row="0"
                                        Text="{Binding CategorySymbol}"
                                        FontFamily="{StaticResource FontAwesomeSolid}"
                                        FontSize="50" />
                                        <StackLayout Grid.Column="1" Grid.Row="0" HorizontalOptions="FillAndExpand">
                                            <Label Style="{StaticResource BaseLabel}" Text="{Binding Name}" FontAttributes="Bold" />
                                            <StackLayout Orientation="Horizontal">
                                                <Label Style="{StaticResource BaseLabel}" Text="{Binding PointsText}" FontSize="Small" HorizontalOptions="FillAndExpand" />
                                                <Label Style="{StaticResource TagLabel}" Text="{Binding Tags}" FontSize="Small"/>
                                            </StackLayout>
                                        </StackLayout>
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding BindingContext.DetailsCommand, Source={x:Reference Name=page}}"
                                                CommandParameter="{Binding Source={x:Reference Item}, Path=BindingContext}"/>
                                        </Grid.GestureRecognizers>
                                    </Grid>
                                    <BoxView HeightRequest="1" />
                                </StackLayout>
                            </ContentView>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
